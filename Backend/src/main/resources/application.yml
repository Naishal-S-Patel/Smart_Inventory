server:
  port: 8080
  forward-headers-strategy: framework

spring:
  application:
    name: smart-inventory
  datasource:
    url: jdbc:postgresql://localhost:5432/smart_inventory
    username: postgres
    password: naishal@7
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: Ahueygiur127AHbcuyrlzmcorju13940dhzbhmrejhy324u1384fnrm
  access-token-exp-minutes: 15
  refresh-token-exp-days: 7

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

app:
  cors:
    allowed-origins:
      - "http://localhost:3000"
    allowed-methods:
      - "GET"
      - "POST"
      - "PUT"
      - "DELETE"
      - "OPTIONS"
    allowed-headers:
      - "*"
    allow-credentials: true
